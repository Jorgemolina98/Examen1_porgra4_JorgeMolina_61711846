const mongoose = require('mongoose');

let Schema = mongoose.Schema;

//Definir el esquema para el modelo Producto (Valor 5 puntos)
let Producto = new this.schema;



module.exports = mongoose.model('Producto', usuarioSchema);